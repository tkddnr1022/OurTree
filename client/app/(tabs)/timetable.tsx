import { Text, View } from "react-native";

export default function TimeTable() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>시간표</Text>
    </View>
  );
}
